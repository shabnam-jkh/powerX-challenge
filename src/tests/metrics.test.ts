import request from "supertest";
import { app } from "..";

describe('POST /data', () => {
  it('handle invalid data', async () => {

  });

  it('save valid data', async () => {

  });
});

describe('GET /data', () => {
    it('handle invalid date ranges', async () => {
    
  });

  it('successfully return data', async () => {
    
  });
});